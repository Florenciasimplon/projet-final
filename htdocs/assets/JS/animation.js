document.addEventListener('DOMContentLoaded', function() {
  var elems = document.querySelectorAll('.datepicker');
  var instances = M.Datepicker.init(elems, options);
});

// const observer = new IntersectionObserver(entries => {
//     // Loop over the entries
//     entries.forEach(entry => {
//       // If the element is visible
//       if (entry.isIntersecting) {
//         // Add the animation class
//         entry.target.classList.add('active');
//         return
//       }
//       entry.target.classList.remove('active');
      
//     });
//   });

// observer.observe(document.querySelector('#texttroisgros'));
// const observer2 = new IntersectionObserver(entries => {
//     // Loop over the entries
//     entries.forEach(entry => {
//         console.log(entry);
//       // If the element is visible
//       if (entry.isIntersecting) {
//         // Add the animation class
//         entry.target.classList.add('active');
//         return
//       }
//       entry.target.classList.remove('active');
      
//     });
//   });
  
//   observer2.observe(document.querySelector('#picturetroisgros'));
//   /******************Champlong*********************/
//   const observer3 = new IntersectionObserver(entries => {
//     // Loop over the entries
//     entries.forEach(entry => {
//         console.log(entry);
//       // If the element is visible
//       if (entry.isIntersecting) {
//         // Add the animation class
//         entry.target.classList.add('active');
//         return
//       }
//       entry.target.classList.remove('active');
      
//     });
//   });

// observer3.observe(document.querySelector('#picturechamplong'));

// const observer4 = new IntersectionObserver(entries => {
//     // Loop over the entries
//     entries.forEach(entry => {
//       // If the element is visible
//       if (entry.isIntersecting) {
//         // Add the animation class
//         entry.target.classList.add('active');
//         return
//       }
//       entry.target.classList.remove('active');
      
//     });
//   });

// observer4.observe(document.querySelector('#textchamplong'));
// /*******************RONGEFER********************/
// const observer5 = new IntersectionObserver(entries => {
//     // Loop over the entries
//     entries.forEach(entry => {
//       // If the element is visible
//       if (entry.isIntersecting) {
//         // Add the animation class
//         entry.target.classList.add('active');
//         return
//       }
//       entry.target.classList.remove('active');
      
//     });
//   });

// observer5.observe(document.querySelector('#textrongefer'));
// const observer6 = new IntersectionObserver(entries => {
//     // Loop over the entries
//     entries.forEach(entry => {
//       // If the element is visible
//       if (entry.isIntersecting) {
//         // Add the animation class
//         entry.target.classList.add('active');
//         return
//       }
//       entry.target.classList.remove('active');
      
//     });
//   });

// observer6.observe(document.querySelector('#picturerongefer'));
// /*******************PETIT PRINCE***********************/
// const observer7 = new IntersectionObserver(entries => {
//     // Loop over the entries
//     entries.forEach(entry => {
//       // If the element is visible
//       if (entry.isIntersecting) {
//         // Add the animation class
//         entry.target.classList.add('active');
//         return
//       }
//       entry.target.classList.remove('active');
      
//     });
//   });

// observer7.observe(document.querySelector('#textprince'));
// const observer8 = new IntersectionObserver(entries => {
//     // Loop over the entries
//     entries.forEach(entry => {
//       // If the element is visible
//       if (entry.isIntersecting) {
//         // Add the animation class
//         entry.target.classList.add('active');
//         return
//       }
//       entry.target.classList.remove('active');
      
//     });
//   });