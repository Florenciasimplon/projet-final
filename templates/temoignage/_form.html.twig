{{ form_start(form) }}
    {{ form_widget(form) }}
    <input type="hidden" name="idRestaurant" id='idRestaurant' value=7>
    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
